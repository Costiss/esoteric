2026-03-04: Started project scaffolding.
- Marked Task 1 as In Progress in PLAN.md.
- Added repository skeleton files: README.md, LICENSE, CODE_OF_CONDUCT.md, CONTRIBUTING.md.
- Created `backend/` and `mobile/` directories with READMEs.
- Added `backend/Cargo.toml` workspace and `backend/crates/.gitkeep`.
- Created initial `AGENTS.md` with architectural decisions.

Next actions:
- Continue scaffolding: add CI skeleton, initial Diesel migration stub, and basic backend crate structure.

2026-03-04 (continued):
- Marked Task 1 as Done in PLAN.md after confirming repository skeleton exists.
- Updated AGENTS.md with recent progress and next steps.
 
2026-03-04 (edit):
- Added `log` and `env_logger` as dependencies to `backend/crates/common/Cargo.toml` to prepare for structured logging early in the workspace.

Reflections:
- Starting with small, focused common utilities (ULID generator, logging) makes integration in other crates straightforward and reduces churn.

Work performed in this run:
- Added `log` and `env_logger` to `backend/crates/common/Cargo.toml`.
- Extended `backend/crates/common/src/lib.rs` with `init_logging` and `Config::from_env`.
- Added `backend/crates/auth/` crate skeleton with a placeholder lib.

Notes/Next steps:
- Continue scaffolding other crates: `users`, `services`, `bookings`, etc.
- Add CI skeleton and Biome/rustfmt/clippy config next run.

Reflections:
- Scaffolding completed successfully; next focus should be CI configuration and Diesel migrations.
- Prefer Diesel migration examples that use CHAR(26) ULID primary keys and CITEXT email columns.

2026-03-04 (Task 3 completion):
- Completed backend workspace scaffolding by adding db crate with Diesel schema for users and services tables (using ULID CHAR(26) and CITEXT), created src/bin/server.rs with basic Axum setup, and scaffolded all remaining crates (users, providers, services, bookings, payments, notifications, admin) with placeholders.
- Fixed ulid version to 1.2 in common/Cargo.toml.
- Ensured backend compiles successfully with cargo check.

Reflections:
- Scaffolding all crates at once provides a solid foundation for parallel development across modules.
- Manual Diesel schema creation works well when migrations are already defined; future schemas can be generated with diesel print-schema after DB setup.
- Basic server binary with Axum allows for early testing and integration.

Notes/Next steps:
- Task 3 marked as Done in PLAN.md.
- Consider starting Task 4 (IDs & storage ULID) next, as ULID helpers are already in common and schema uses Text for IDs.
- AGENTS.md updated with recent progress if needed.

2026-03-04 (Task 4 in progress):
- Updated Diesel schema.rs to use Char for ULID fields (id and provider_id) to match CHAR(26) in migration.
- Added unit tests for ULID generation in common crate: length, uniqueness, alphanumeric.
- Verified backend compiles and tests pass.

Reflections:
- Using Char in schema.rs for PostgreSQL CHAR fields ensures type safety.
- Tests ensure ULID generation is correct and deterministic in format.

2026-03-04 (Task 2 in progress):
- Checked installed tools: Rust (1.92.0), Node.js (24.11.1), npm (11.6.2), PNPM (10.23.0), PostgreSQL client (17.8); missing Yarn, Expo CLI, Diesel CLI.
- User needs to install missing tools: 
  - Yarn: npm install -g yarn
  - Expo CLI: npm install -g @expo/cli
  - Diesel CLI: cargo install diesel_cli --no-default-features --features postgres
- Added Biome configuration to mobile/biome.json with strict TypeScript rules, including noExplicitAny error to prohibit 'any' type.
- Updated CI workflow (.github/workflows/ci.yml) to separate jobs for backend (Rust: fmt check, clippy, tests) and mobile (Node: Biome check), using modern GitHub Actions.
- Committed changes.

Reflections:
- CI skeleton now properly set up for both backend and mobile with appropriate tools.
- Biome config enforces type safety as required.
- Next steps: User installs missing tools, then initialize Expo app in mobile/ with TypeScript strict mode.

Notes/Next steps:
- Continue Task 2 by initializing Expo app once tools are installed.
- Consider adding rustfmt.toml if custom formatting needed, but defaults suffice for now.

2026-03-04 (Task 2 - dev env docs):
- Expanded root README with development environment prerequisites (Rust, Node,
  Expo CLI, Diesel CLI), instructions for running Postgres and Valkey via
  `backend/docker-compose.yml`, and an overview of backend and mobile
  workflows and CI.
- Updated `backend/README.md` with concrete commands for starting Docker
  services, applying Diesel migrations, and running the server, tests, fmt,
  and clippy.
- Updated `mobile/README.md` to document current status (Expo app not yet
  initialized), prerequisites for mobile dev, and how to run Biome locally.

Reflections:
- Writing focused dev environment docs early reduces friction when returning
  to the project or onboarding new contributors.
- Keeping Task 2 open until the Expo app is scaffolded and dev tooling is
  exercised end-to-end feels appropriate; the current changes mainly clarify
  how to use what is already in place.

2026-03-04 (Task 2 completed):
- Initialized Expo app in mobile/ directory using npx create-expo-app (via temp directory due to existing files).
- Installed @tamagui/core and @tamagui/babel-plugin for UI component library.
- Created babel.config.js with Tamagui babel plugin and reanimated plugin.
- Created tamagui.config.ts with custom esoteric theme tokens (purple/violet colors for stardust theme).
- Updated package.json: renamed to "esotheric-mobile", added biome lint/format scripts.
- Installed @biomejs/biome as dev dependency.
- Fixed biome.json to use correct schema version (2.4.5) and updated deprecated keys.
- Verified TypeScript strict mode works (npx tsc --noEmit passes).
- Verified Biome linting works - noExplicitAny rule enforced, no explicit any types found in codebase.

Reflections:
- Expo SDK 54 with React Native 0.81.5 provides solid foundation for mobile development.
- Tamagui configuration with custom theme tokens enables consistent esoteric design system.
- Biome linting with noExplicitAny ensures type safety as required by project.

Notes/Next steps:
- Task 2 marked as Done in PLAN.md.
- Next: Start Task 5 (Valkey cache client) or Task 6 (Authentication & OAuth2 server) depending on priorities.

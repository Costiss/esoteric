[package]
name = "bookings"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
db = { path = "../db" }
users = { path = "../users" }
services = { path = "../services" }